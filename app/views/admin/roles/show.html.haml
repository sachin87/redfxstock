.page-header
  %h1=t '.title', :default => 'Role'

%p
  %strong Name:
  = @resource.name

%br

= link_to('Back', admin_roles_path, class: 'btn')
= link_to('Edit', edit_admin_role_path(@resource), class: 'btn')
= link_to('Delete', admin_role_path(@resource), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger')
