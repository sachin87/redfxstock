<% if @upload.new_record? %>
  alert("Failed to upload painting: <%= j @upload.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#uploads").append("<%= j render(@upload) %>");
<% end %>