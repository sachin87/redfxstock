.page-header.row
  %h1=t '.title', :default => 'Upload'
  = link_to('New Gallery', new_upload_path, :class => 'btn btn-primary pull-right')
  = paginate @uploads
%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Gallery
      %th Image
      %th Owner
      %th Created At
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - @uploads.each do |upload|
      %tr
        %td= link_to upload.name, upload_path(upload)
        %td= upload.gallery.name
        %td= image_tag upload.image
        %td= upload.user_name
        %td=l upload.created_at
        %td
          = link_to('Edit', edit_upload_path(upload), :class => 'btn btn-mini')
          = link_to('Delete', upload_path(upload), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-mini btn-danger')

= paginate @uploads