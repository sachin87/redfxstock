.page-header.row
  %h1=t '.title', :default => 'Categories'
= paginate @categories

%table.table.table-striped
  %thead
    %tr
      %th Name
  %tbody
    - @categories.each do |category|
      %tr
        %td= link_to category.name, category_path(category)

= paginate @categories
